# Production (runtime) dependencies - OPTIMIZED (reduced from 2GB to ~600MB)

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# Redis & Cache
redis==5.0.1
hiredis==2.2.3

# Task Queue
celery==5.3.4
flower==2.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
bcrypt==4.1.1
email-validator==2.1.0

# Image Processing (OPTIMIZED: headless saves ~70MB)
Pillow==10.1.0
pillow-heif==0.14.0
numpy==1.24.3
matplotlib==3.10.8  # Used for AI data visualization charts (_create_chart)

# Document Processing (HEAVILY OPTIMIZED)
# REMOVED pdf2docx (requires opencv-python + PyMuPDF = 230MB bloat!)
# Use Adobe PDF Services or pypdfium2 for PDF to Word conversion instead
opencv-python-headless==4.11.0.86  # Headless version (no GUI) saves ~70MB vs opencv-python
pypdf==4.0.0
# pdf2docx==0.5.6  # REMOVED: pulls opencv-python + PyMuPDF (230MB)
python-docx==1.1.0
docxtpl==0.16.8
python-pptx==0.6.23
openpyxl==3.1.2
pdfplumber==0.10.3
img2pdf==0.5.0
reportlab==4.0.7
pdf2image==1.17.0
pypdfium2==4.26.0
aiofiles==23.2.1

# Adobe PDF Services (optional - high quality PDF to Word conversion)
pdfservices-sdk==4.1.0

# Google Gemini AI
google-generativeai==0.3.2

# OCR
pytesseract==0.3.10

# Text Processing - REMOVED (not used in code)
# nltk==3.8.1  # REMOVED: saves ~14MB
# textblob==0.17.1  # REMOVED: saves ~2MB

# HTTP & Networking
httpx==0.25.1
aiohttp==3.9.1
requests==2.31.0

# Utilities
python-magic==0.4.27
filetype==1.2.0
shortuuid==1.0.11

# Monitoring & Logging
loguru==0.7.2
prometheus-client==0.19.0
